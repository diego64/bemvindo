apiVersion: 1

datasources:
  - name: Loki
    uid: loki-bv
    type: loki
    access: proxy
    url: http://bv-loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000
      derivedFields:
        - name: reqId
          matcherRegex: '"reqId":"([^"]+)"'
          url: ''
    editable: false
